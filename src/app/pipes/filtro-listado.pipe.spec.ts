import { FiltroListadoPipe } from './filtro-listado.pipe';

describe('FiltroListadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroListadoPipe();
    expect(pipe).toBeTruthy();
  });
});
